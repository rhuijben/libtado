.. _installation:

=======================
Installation
=======================

You can download the official library on Pypi using `pip`.

.. code-block:: bash

    pip install libtado.

It cannot be easier ;)
